 <%= form_with model: @profile do |f| %> 
  <div class="field">
    <%= f.label :tagline %><br />
    <%= f.text_field :tagline %>
  </div>

    <div class="field">
    <%= f.label :interests %><br />
    <%= f.text_field :interests %>
    </div>

    <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>